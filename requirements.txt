fastapi==0.136.1                # 0.116+ pulls starlette>=0.46 (we need >=0.49.1 for CVE-2025-62727); 0.136 pulls starlette 1.0.0
uvicorn[standard]==0.32.1
python-multipart==0.0.27        # 0.0.22+ fixes CVE-2026-24486 (HIGH path traversal); 0.0.26 added CVE-2026-40347 (MEDIUM DoS) fix; 0.0.27 fixes GHSA-pp6c-gr5w-3c5g (multipart parser DoS)
jinja2==3.1.6                   # 3.1.5+ fixes CVE-2025-27516 (sandbox escape via attr filter)
sqlalchemy==2.0.49             # 2.0.49 broadens Session.execute() return type to Result[Any] — handled in app/retention.py + app/master/trainline.py via cast (audit-2026-05 #23)
psycopg[binary]==3.2.3
pydantic==2.10.3
pydantic-settings==2.6.1
alembic==1.14.0
python-jose[cryptography]==3.5.0   # 3.4.0+ fixes CVE-2024-33664 + CVE-2024-33663 (algorithm confusion)
passlib[bcrypt]==1.7.4
bcrypt==4.0.1                   # passlib 1.7.4 needs bcrypt < 4.1
slowapi==0.1.9
email-validator==2.2.0          # required by pydantic EmailStr
aiosmtplib==3.0.2               # used for SMTP delivery in step 4
httpx==0.28.1                   # async HTTP for Trainline pull + OTP fanout
apscheduler==3.10.4             # in-process scheduler for retention + master-data refresh crons
structlog==24.4.0               # JSON structured logging — config in app/logging_config.py; bridges stdlib logging + uvicorn/SQLAlchemy/APScheduler emitters into a single JSON stream
prometheus-client==0.21.1       # Prometheus exposition format + custom collectors (audit #14 — observability triad). HTTP middleware is hand-rolled in app/metrics.py instead of prometheus-fastapi-instrumentator (which pins starlette<1.0.0 and would conflict with our starlette==1.0.0 floor for CVE-2025-62727).
