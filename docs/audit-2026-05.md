# Platform audit — VIATOR / OpenJourneyPlanner

| Field | Value |
|-------|-------|
| Date | 2026-05-06 |
| Version audited | v0.1.32.1 |
| Scope | Code maintainability, code performance, security, GitHub/DevOps integration |
| Method | Read-only static survey of `app/`, `tests/`, `docker/`, `.github/`, `pyproject.toml`, `requirements*.txt`, `coverage.xml`, `docs/admin-guide.md`. No runtime profiling, no penetration testing, no GitHub-API inspection of branch protection. |
| Auditor | Self-audit assisted by Claude (Opus 4.7) |

---

## Executive summary

The platform has a **disciplined development discipline** (ruff strict, mypy strict, pre-commit, Bandit, Trivy gate, pinned deps, pydantic everywhere) but a **lopsided risk profile**: code-quality gates are thorough, while runtime confidence (test coverage) and container/supply-chain hardening are weak. Release velocity is high (~32 tags) but tooling for visibility (logs, metrics, traces) and dependency hygiene (Dependabot, lock file, SBOM) hasn't kept pace.

**Single biggest concern:** test coverage at **13.29% line / 3.22% branch** despite 235 tests existing. The strict static gates can't catch what tests don't exercise — the team is essentially shipping by manual operator confidence.

**Single biggest quick win:** both Docker images run as root with no `USER` directive. One-line fix per Dockerfile, large security ROI.

---

## 1. Code maintainability

### Strengths

- Mypy `strict=true` with only **6 `# type: ignore`** across ~13k LOC of `app/` — exceptionally low escape rate.
- Ruff rule set is broad and modern (E, W, F, I, B, C4, UP, SIM, RET, PIE, S, PTH, RUF) with thoughtful per-file relaxations (tests/ ignore S105/S106/S311).
- Pre-commit covers format/lint + secret detection (`detect-private-key`) + Hadolint; mypy runs at pre-push (correct trade-off: fast commits, gated push).
- Only **3 TODO/FIXME** in `app/`, all linked to deferred-step plans (not rot):
  - [`app/main.py:115`](../app/main.py) — FIXME(step-3/-7) auth + sessions wiring
  - [`app/models/ingestion.py:138`](../app/models/ingestion.py) — FIXME(step-7) session_id NOT NULL
  - [`app/models/ingestion.py:142`](../app/models/ingestion.py) — FIXME(step-3) user_id NOT NULL
- Pydantic v2 with `pydantic.mypy` plugin — request/response contracts are type-checked end-to-end (46 `BaseModel` subclasses).

### Risks

- **`app/api/admin/sessions.py` is 2,019 LOC** — mixes session CRUD, OSM rotation, rebuild orchestration, and archival. Five functions over 130 LOC each:
  - `patch_session()` (~154 LOC)
  - `delete_session()` (~141 LOC)
  - `archive_session()` (~174 LOC)
  - `_reconstruct_dispatch_target()` (~137 LOC)
  - `_rotate_osm_pbf()` (~142 LOC)

  Single-file change risk + review fatigue.
- **Test coverage 13.29% line / 3.22% branch** despite 235 tests existing. With a strict-typed codebase, the *type* of bug that escapes is precisely the runtime/business-logic kind tests catch.
- **SonarCloud is wired but dormant** — `sonar-project.properties` exists, but the workflow gate is conditional on `SONARCLOUD_ENABLED` and the variable isn't set. Paid-for analysis not running.

### Code-size summary

| Area | LOC | Files |
|------|-----|-------|
| `app/` total | 12,991 | 69 |
| `app/api/` | 5,421 | — |
| `app/` root | 3,892 | (main.py, worker.py, ingestion.py, db.py, settings.py, …) |
| `app/models/` | 1,278 | — |
| `app/master/` | 908 | — |
| `app/network_coverage/` | 741 | — |
| `app/journey/` | 445 | — |
| `app/auth/` | 306 | — |
| `tests/` | 5,507 | 35 |

---

## 2. Code performance

### Strengths

- OTP client uses `httpx.AsyncClient` ([`app/journey/otp_client.py:131`](../app/journey/otp_client.py)) — the single most latency-sensitive integration is non-blocking.
- 46 Pydantic models ⇒ validation is C-accelerated, not hand-rolled.
- `pool_pre_ping=True` on the engine ([`app/db.py:17`](../app/db.py)) — avoids stale-connection pitfall.
- Trivy + Hadolint already gating; layer count in both Dockerfiles is reasonable (~15 web, ~6 OTP).
- BuildKit cache (`cache-from`/`cache-to: type=gha`) wired in `docker.yml`.

### Risks

- **Sync SQLAlchemy 2.0.36 under async FastAPI**: 219 `def` vs 38 `async def` (82.7% sync). Every DB-touching route blocks a worker thread. Under any meaningful concurrency, this is the throughput ceiling — not OTP, not network, but the Python event loop being held by `session.execute()`.
- **Zero Python-side caching**: no `functools.lru_cache`, no Redis client, no ETag/Cache-Control middleware. Reference data (master lists, NAP catalogue, configs) is re-fetched per request. Redis is referenced in `docker-compose.yml` comments but not wired.
- **No HEALTHCHECK** in either Dockerfile → Compose / orchestrator can't distinguish "started" from "ready"; rolling deploys are blind to in-flight readiness.
- N+1 query risk: spot-checks of `app/api/admin/sessions.py` looked clean, but the full surface wasn't traced — there's no test asserting query counts.
- No load-test artefacts under `tests/` — no signal on actual latency budget.

---

## 3. Security

### Strengths

- All 12 prod + 14 dev deps pinned `==` (deterministic builds, no version drift on rebuild).
- pydantic-settings + `.env.example` pattern is correct; no hardcoded credentials in `app/`.
- Bandit on every PR (`-ll` = LOW+ severity); Trivy two-pass design (table-gating + non-fatal SARIF) is post-v0.1.25 sound — see [admin-guide §6.9](admin-guide.md).
- `.trivyignore` has per-CVE justifications grounded in actual usage (CLI subprocess, localhost socket only) — 6 CVEs accepted, all in upstream `docker:29-cli`.
- 127 `Depends(...)` injections + role gates (`require_platform_admin`, `require_content_manager`, `require_logged_in`) — auth surface is centralized.
- HTTP Basic + JWT coexist (Phase-1 + Phase-2); JWT extraction checks cookie then Bearer header ([`app/security.py:97-158`](../app/security.py)).

### Risks

- **Both containers run as root** — no `USER` directive in [`docker/web/Dockerfile`](../docker/web/Dockerfile) or [`docker/otp/Dockerfile`](../docker/otp/Dockerfile). Single RCE = root in container; the web container has the docker socket mounted (per worker `otp-build` flow), so container escape on RCE = host root.
- **All 16 third-party actions are tag-pinned, none SHA-pinned** — the v5/v6/v4/`@v0.36.0` tags can be force-updated by the action publisher (or compromised). For a release pipeline that pushes to GHCR, this is the primary supply-chain attack vector.
- **No lock file** (`requirements.txt` is the only pin manifest). Transitive deps not pinned ⇒ `pip install -r` today and tomorrow can resolve different transitive trees.
- **`pip-audit` is in dev deps but not invoked in CI** ([`.github/workflows/ci.yml`](../.github/workflows/ci.yml)) — Python-layer CVEs aren't gated; only OS/library layer (via Trivy on the built image) is.
- No CodeQL or Semgrep — Bandit is solid for low-hanging fruit but doesn't catch dataflow-grade issues (taint, deserialization).
- No SBOM generation (CycloneDX/Syft).
- JWT default secret in [`app/settings.py:39`](../app/settings.py) is the literal `"change-me-in-prod-use-32-bytes-random"` — no startup assertion enforces "if PROD, this must differ from default".
- No CORS middleware configured (correct *for now* given same-origin deployment, but should be commented as intentional, not forgotten).

---

## 4. GitHub / DevOps integration

### Strengths

- Two-workflow split is clean: lint/type/test in [`ci.yml`](../.github/workflows/ci.yml), image build/scan in [`docker.yml`](../.github/workflows/docker.yml).
- Concurrency cancellation set on `docker.yml` (avoids redundant builds on rapid pushes).
- Coverage XML + Trivy SARIF uploaded as workflow artifacts (auditable, downloadable).
- GHCR + semver tagging stack is correct.
- Per-image `vuln-type` (`os,library` for web, `os` only for OTP) reflects actual ownership of the dep tree — thoughtful.

### Risks

- **No `.github/dependabot.yml`** → 12 prod deps + 14 dev deps + GitHub actions all updated by hand. At ~30 releases over a year, dep drift is accumulating silently.
- **No CODEOWNERS, no PR template, no issue templates** — review routing and incident-report intake are ad-hoc.
- **No release automation** (release-please / semantic-release / changesets). Tagging is manual; release-note quality depends on commit-message hygiene (which is currently good — but it's not load-bearing on automation).
- **No observability stack**: stdlib `logging` only (14 imports), no `structlog`, no `prometheus_client`, no `opentelemetry`. For a platform that runs long OTP graph builds (admin-guide §6.2 documents `OOM-killed (exit 137)` as a known case), absence of metrics is a real operational gap.
- **SonarCloud job is dormant** — `if: vars.SONARCLOUD_ENABLED == 'true'` and the var isn't set.
- Branch protection not inspectable from disk — needs `gh api` confirmation that `ci`, `docker / build-and-scan (web)`, `docker / build-and-scan (otp)`, and `hadolint` are required checks on `main`.
- `docker.yml` runtime ~30 min on tagged releases (matrix × build+scan); cache may not be hitting consistently — worth monitoring `cache-from` hit-rate in build logs.

### Workflow inventory

| File | Triggers | Jobs | Est. runtime |
|------|---------|------|--------------|
| `.github/workflows/ci.yml` | PR, push to `main` | python (lint + type + bandit + pytest), sonarcloud (dormant), pre-commit | ~5 min |
| `.github/workflows/docker.yml` | push to `main`, tag `v*`, PR on `docker/`+`app/`+`requirements.txt`, manual | build-and-scan (matrix: web, otp), hadolint | ~30 min on tag |

---

## Action plan

Effort labels: **S** ≤ ½ day · **M** 1–3 days · **L** > 3 days.

### P0 — do first (security floor + release confidence)

| # | Item | Effort | Why |
|---|------|--------|-----|
| 1 | Add non-root `USER` (fixed UID) to both Dockerfiles + `chown` app dirs | S | Single biggest container-security improvement; trivial to test |
| 2 | Add Dependabot config covering `pip` (req.txt + req-dev.txt), `docker` (both Dockerfiles), `github-actions` | S | Restores the steady drumbeat of patch-grade updates without operator effort |
| 3 | Wire `pip-audit` into `ci.yml` (already a dev dep) — fail on HIGH/CRITICAL, allowlist via `pyproject.toml` | S | Closes the Python-layer CVE gap (Trivy only catches OS-layer in built images) |
| 4 | Coverage push: pick 5 highest-LOC modules under `app/api/` and `app/journey/` and write integration tests until each crosses 60% line. Update `ci.yml` to fail under a coverage floor (start at 30%, ratchet up) | L | Without this, every other gate is decoration. Strict typing/linting catches surface bugs; tests catch the rest |
| 5 | Activate SonarCloud (set `SONARCLOUD_ENABLED=true` repo var, confirm token) | S | Already paid for; immediate findings flow at zero marginal cost |

### P1 — within the next 1–2 sprints

| # | Item | Effort | Why |
|---|------|--------|-----|
| 6 | Split `app/api/admin/sessions.py` (2,019 LOC) into 4 modules: CRUD, archival, OSM rotation, dispatch reconstruction | M | Reduces review-load and makes safe parallel work on session features feasible |
| 7 | SHA-pin all third-party actions (`docker/login-action`, `docker/build-push-action`, `aquasecurity/trivy-action`, `github/codeql-action`, `hadolint/hadolint-action`, `SonarSource/sonarcloud-github-action`) — let Dependabot's action-update PRs keep them current | S | Closes the supply-chain tag-mutation vector |
| 8 | Add `HEALTHCHECK` to both Dockerfiles (web: `curl -f localhost:8000/healthz`; OTP: `curl -f localhost:8080/otp/`) | S | Compose / orchestrator readiness becomes truthful |
| 9 | Decide async DB story explicitly: either (a) migrate to `sqlalchemy[asyncio]` + `asyncpg` and `async def` the route layer, or (b) document that DB is intentionally sync and sized for low concurrency. **Don't leave undecided** — current state is "async framework holding sync DB" which is the worst of both | L | Throughput ceiling. Even "we accept it, here's why" is a valid outcome — the issue is silence, not the choice |
| 10 | Generate a lock file: `pip-compile` from a `requirements.in`, commit `requirements.lock` with hashes, install with `--require-hashes` in CI + Dockerfile | M | Pins transitive deps + blocks supply-chain swap on any of them |
| 11 | Add CODEOWNERS, PR template (link to admin-guide §X for ops checks), bug + ops issue templates | S | Inbound-channel hygiene |
| 12 | Verify branch protection on `main` via `gh api`: required checks = `ci`, `docker / build-and-scan (web)`, `docker / build-and-scan (otp)`, `hadolint`; disallow force-push, require review. Document the expected config in this audit | S | Configuration-as-documented; catches accidental loosening |

### P2 — strategic improvements

| # | Item | Effort | Why |
|---|------|--------|-----|
| 13 | Introduce structured logging (`structlog`, JSON output to stdout) + a minimal request-id middleware | M | Operations leverage. At current platform complexity flat logs are already painful |
| 14 | Add `prometheus_client` + `prometheus-fastapi-instrumentator` for HTTP metrics + custom counters for OTP build duration, ingestion durations, queue depth | M | Metrics-first incident response replaces "tail the log and grep" |
| 15 | Add CodeQL workflow (free for public repos) | S | Catches what Bandit misses (taint, deserialization, path traversal) |
| 16 | Add SBOM generation: `cyclonedx-python` for the wheel layer + `anchore/sbom-action` for the image. Attach to release | S | Required for downstream consumers; nice-to-have today, table-stakes within ~12 months |
| 17 | Release automation: `release-please` (Conventional-Commits-friendly given existing commit style). Auto-PR for release bump, auto-tag on merge | M | Removes the only manual step in the ship flow |
| 18 | Caching layer: wire the Redis service already in `docker-compose.yml`. Cache: NAP catalogue, master station list, OTP graph metadata. Adopt `fastapi-cache2` or hand-roll | M | Reduces DB load + per-request latency on read-heavy admin pages |
| 19 | Add minimal OTel tracing exporting to OTLP (Jaeger/Tempo). One trace per request through FastAPI → DB → OTP makes admin-guide §6.7 debugging a 30-second job | M | Pairs with #14; both together replace the bulk of admin-guide's troubleshooting matrix |
| 20 | Add a `make test-load` (locust or k6) that exercises journey-search at realistic concurrency and asserts p95 latency. Run on a manual workflow, not every PR | M | Catches the sync-DB ceiling regression *before* customers do |

---

## What's already good — don't over-correct

- The strict mypy + ruff + Bandit + Trivy + Hadolint stack is mature — don't churn it.
- `==`-pinning + Trivy SARIF + table-gating-with-severity is the right two-pass design (the v0.1.25 fix earned that).
- The two-image build matrix with per-image `vuln-type` and per-image `.trivyignore` semantics is genuinely thoughtful.
- The admin-guide is a real asset — keep it as the source of operational truth; metrics and structured logs should *augment* it, not replace it.
- Pre-commit configuration (mypy at pre-push, lint/format/secret-detection at pre-commit) reflects practical experience — keep that split.

---

## Tracking

| ID | Title | Priority | Status | Owner | Closed in version |
|----|-------|----------|--------|-------|-------------------|
| 1 | Non-root USER in both Dockerfiles | P0 | ✅ **closed in v0.1.32.3** (deployed; verified `uid=1000` on prod) | — | v0.1.32.3 |
| 2 | Dependabot config | P0 | ✅ **closed in v0.1.32.3** (active; produced 9 PRs in first run) | — | v0.1.32.3 |
| 3 | pip-audit in CI | P0 | ✅ **closed in v0.1.32.3** (caught + triaged 4 CVEs on first run) | — | v0.1.32.3 |
| 4 | Coverage push + floor | P0 | 🟡 **floor wired @ 12% in v0.1.32.3** (regression gate); test-writing campaign still open | — | v0.1.32.3 (floor only) |
| 5 | Activate SonarCloud | P0 | ✅ **closed in v0.1.32.3** (active; PR-level + main-branch analysis running) | — | v0.1.32.3 |
| 6 | Split sessions.py | P1 | open | — | — |
| 7 | SHA-pin third-party actions | P1 | ✅ **closed in v0.1.32.4** (12 actions pinned across both workflows) | — | v0.1.32.4 |
| 8 | Dockerfile HEALTHCHECKs | P1 | open | — | — |
| 9 | Async DB decision | P1 | open | — | — |
| 10 | Lock file + hash-checking install | P1 | open | — | — |
| 11 | CODEOWNERS + PR/issue templates | P1 | 🟡 **implemented, awaiting merge** — `.github/CODEOWNERS` (default + high-trust paths), `.github/PULL_REQUEST_TEMPLATE.md` (Summary/Audit ref/Test plan/Risk/Migration/Post-merge), three issue templates (bug / ops_incident / feature) + `config.yml` linking to admin-guide and audit tracker. The ops_incident template is structured to make post-mortems quick to write and patterns easy to spot. | — | — |
| 12 | Verify + document branch protection | P1 | 🔵 **operator action — UI**: required checks should be `Python (lint + type + test)`, `Pre-commit (belt-and-braces)`, `Hadolint`, `Build, scan, push (web)`, `Build, scan, push (otp)`, `SonarCloud Code Analysis`. Disallow force-push, require ≥1 review. The 4 issues caught by audit gates on PR #1 confirm this is high-value. | — | — |
| 13 | Structured logging | P2 | open | — | — |
| 14 | Metrics (Prometheus) | P2 | open | — | — |
| 15 | CodeQL workflow | P2 | open | — | — |
| 16 | SBOM generation | P2 | open | — | — |
| 17 | Release automation | P2 | open | — | — |
| 18 | Caching layer (Redis) | P2 | open | — | — |
| 19 | OTel tracing | P2 | open | — | — |
| 20 | Load-test workflow | P2 | open | — | — |
| 21 | Bump black 24.10.0 → 26.x (ReDoS GHSA-3936-cmfr-pm3m, dev-only) | P1 | open | — | — |
| 22 | Bump pytest 8.3.4 → 9.x (test-isolation GHSA-6w46-j5rx-g56g, dev-only) | P1 | open | — | — |
| 23 | Adopt sqlalchemy 2.0.49+ Result typing (cast to CursorResult for `rowcount` access) | P1 | open — surfaced when Dependabot grouped runtime bump (PR #6, closed) tripped mypy strict on 6 sites: `app/master/trainline.py:249`, `app/retention.py:{52,66,78,83,88}`. Mechanical fix; once landed, the 7-package grouped runtime bump (uvicorn, psycopg, pydantic, pydantic-settings, alembic, email-validator, apscheduler) auto-merges on next Dependabot run. | — | — |
| 24 | Migrate str-Enum classes to `enum.StrEnum` (ruff UP042) | P1 | open — surfaced when Dependabot grouped dev bump (PR #7, closed) activated ruff UP042 rule on the new ruff version. 17 sites across `app/models/identity.py`, `app/models/sessions.py`, `app/api/journey.py`, `app/settings.py`. We're on Python 3.12 so `enum.StrEnum` is available. | — | — |
| 25 | Worker orphan-cleanup misses Exited containers (`docker ps` was running-only) | P2 | 🟡 **implemented, awaiting merge** — root cause: `app/worker.py:_apply_reload` used `docker ps` (running only) for orphan detection. `Exited (143)` containers from prior-deploy SIGTERMs slipped through and accumulated indefinitely (the 2026-05-07 prod incident had two such orphans 21–25 hours old). Fix: `docker ps -a`, extract `_parse_otp_service_names` for unit-testability, skip ephemeral `viator-otp-build-run-*` containers. Runbook step added in admin-guide §6.11 step 7 documenting the post-deploy verification operators should run. | — | — |

### Post-merge release log

| Version | Date | Audit items shipped | Notes |
|---------|------|---------------------|-------|
| v0.1.32.3 | 2026-05-07 | P0 #1, #2, #3, #5; P0 #4 (regression gate only) | Non-root containers + Dependabot + pip-audit + coverage floor + SonarCloud activation. Deployed to prod, `uid=1000` verified. |
| v0.1.32.4 | 2026-05-07 | P1 #7 + post-merge SSRF/log-injection fix (SonarCloud finding) | All 12 third-party actions SHA-pinned with `# vN` version comments. nap_importer.py SSRF-hardened with `_validate_safe_http_url` + manual redirect handling. |

When closing items, set **Closed in version** to the release tag and update the relevant pillar section above with a one-line note (`✓ closed in vX.Y.Z`).

### Accepted Python CVEs (pip-audit ignores)

Mirror of `.trivyignore`. Each CVE here is permanently recorded with a written justification; remove the entry when the underlying dep is bumped or the upstream advisory withdrawn.

| CVE / GHSA | Package | Type | Justification | Re-evaluate when |
|-----------|---------|------|---------------|------------------|
| GHSA-wj6h-64fc-37mp | ecdsa 0.19.2 | transitive (python-jose[cryptography]) | Timing side-channels in ECDSA sign/verify. Upstream declined to fix. We use python-jose with the cryptography backend (`[cryptography]` extra) which forces the cryptography library as the JWS backend; ecdsa wheel is installed but unused on the hot path. JWT signing uses HS256 (see `app/security.py`). | Switching JWT alg to ES256/ES384, or replacing python-jose with `pyjwt`. |
| GHSA-3936-cmfr-pm3m | black 24.10.0 | dev-only | Formatter ReDoS via crafted docstring. Black formats our own source in CI and dev — no untrusted input. | Bump to black 26.x lands (tracked as item #21). |
| GHSA-6w46-j5rx-g56g | pytest 8.3.4 | dev-only | Test-isolation issue. Pytest runs only on our test files in CI. | Bump to pytest 9.x lands (tracked as item #22). |

### P0 implementation log — 2026-05-06

**Files changed (uncommitted, awaiting operator review):**

| File | Change | Linked item |
|------|--------|-------------|
| `docker/web/Dockerfile` | Add `appuser` (UID/GID 1000), pre-create `/data/{inbox,graphs,generated}` and `/srv/docker`, chown, `USER appuser:appuser` at end | #1 |
| `docker/otp/Dockerfile` | Add `appuser` (UID/GID 1000), pre-create `/var/otp/{inbox,graph}`, chown `/opt/otp` + `/var/otp`, `USER appuser:appuser` at end | #1 |
| `docker/docker-compose.yml` | `worker` service gets `group_add: ["${DOCKER_GID:-999}"]` so the non-root user can read the docker socket | #1 |
| `docker/.env.example` | New `DOCKER_GID` variable + comment explaining how to find it (`getent group docker \| cut -d: -f3`) | #1 |
| `docs/admin-guide.md` | New §6.12 "Upgrading past the non-root-container migration" — host-side `chown` + `DOCKER_GID` set + verification steps | #1 |
| `.github/dependabot.yml` | New file — pip (weekly), docker × 2 (monthly per-Dockerfile), github-actions (weekly), grouped patch/minor | #2 |
| `.github/workflows/ci.yml` | New `pip-audit --strict --vulnerability-service=osv` step before pytest; `pytest --cov-fail-under=12` to gate regression | #3, #4 |

**Operator action items before merging:**

1. Read admin-guide §6.12. The non-root migration is **not transparent** — existing prod deployments need to chown `./generated` to UID 1000 and set `DOCKER_GID` in `.env` *before* `docker compose pull && up -d` on the new image.
2. To activate SonarCloud (item #5): GitHub repo → Settings → Variables → set `SONARCLOUD_ENABLED=true`. Then Secrets → add `SONAR_TOKEN` from sonarcloud.io. The workflow will start running on the next push to `main`.
3. The coverage floor of 12% is a *regression gate*, not a target — the test-writing campaign (item #4) is the L-effort follow-up. Treat any PR that lowers actual coverage as a defect, even if it stays above 12%.

**Verification suggested before merging the workflow change:**

- Run the new `ci.yml` on a no-code-change PR to confirm `pip-audit` and the coverage gate pass on the current dep tree at v0.1.32.1.
- Local Docker build of both images + `docker exec <container> id` to confirm UID 1000 is in effect (see admin-guide §6.12 step 4).

### What the audit's CI gates caught on first run

PR #1 surfaced 4 categories of pre-existing issue that previous CI configurations weren't blocking — exactly what the gates were designed to do:

1. **OTP base image regression on `noble`**: `eclipse-temurin:25-jre-noble` ships with a default `ubuntu` user at UID/GID 1000:1000, colliding with our new `appuser`. Fixed in commit `f81ed54` (`userdel --remove ubuntu` before our user-creation block).
2. **4 pip-audit findings**: 1 real production CVE (python-multipart 0.0.26 → 0.0.27, GHSA-pp6c-gr5w-3c5g, multipart-parser DoS); 3 dev-only / transitive ignored with justification (see "Accepted Python CVEs" table below).
3. **2 stale tests** that had been silently broken since at least v0.1.24/v0.1.30:
   - `tests/unit/test_osm_filter.py::TestPresetShape::test_three_scopes_exist` asserted the pre-v0.1.30 set of 3 scopes; `rail-focused` had been live in `app/osm_filter.py` for 4 releases.
   - `tests/unit/test_router_config.py::test_routing_defaults_include_access_egress_bound` asserted the OTP 2.x flat structure; `app/router_config.py` migrated to OTP 2.9's nested shape in commit `fa61df0` and the test wasn't updated. The 20-minute walk-radius safety net (against silent route-truncation to far destinations) had been a no-op in the test suite since v0.1.24.
   Both fixed in this PR.
4. **CI was demonstrably non-blocking on `main`**: items 3.a and 3.b would have failed every PR since v0.1.30 / v0.1.24 respectively if the python job were a required check. This corroborates audit item #12 (verify branch protection) — should be confirmed via `gh api` and tightened immediately post-merge.
