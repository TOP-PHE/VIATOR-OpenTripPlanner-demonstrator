# SonarCloud configuration for VIATOR.
# Set up the SONAR_TOKEN repo secret before the workflow can publish.

sonar.projectKey=TOP-PHE_VIATOR-a-MERITS-OpenTrip-Planner-demonstrator
sonar.organization=top-phe
sonar.projectName=VIATOR
sonar.projectVersion=0.1.0

# Sources and tests
sonar.sources=app
sonar.tests=tests
sonar.python.version=3.12

# Coverage report ingestion
sonar.python.coverage.reportPaths=coverage.xml
# JS coverage joins later when journey-ui/ exists:
# sonar.javascript.lcov.reportPaths=journey-ui/coverage/lcov.info

# Exclusions
sonar.exclusions=\
    docker/otp/**,\
    **/__pycache__/**,\
    **/migrations/versions/**,\
    **/node_modules/**,\
    branding/**

sonar.coverage.exclusions=\
    tests/**,\
    **/migrations/**,\
    **/__init__.py

# Copy-paste detection: integration tests legitimately repeat the
# fresh-DB / TestClient / bootstrap-admin fixture harness across modules
# (a pre-existing pattern across tests/integration/*). Exclude tests from
# CPD so that fixture boilerplate doesn't fail the duplication gate.
sonar.cpd.exclusions=\
    tests/**
